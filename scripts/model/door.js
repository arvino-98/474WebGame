function Door(id, width, height, hboxWidth, hboxHeight, xPos, yPos, open) {
    this.id = id;

    this.width = width;
    this.height = height;
    this.hitboxWidth = hboxWidth;
    this.hitboxHeight = hboxHeight;
    this.xPos = xPos;
    this.yPos = yPos;
    this.open = false;
}